<nav class="navbar navbar-default no-padding">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"><%#= image_tag('mark-github.svg', size: 28) %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/categories"><span class="glyphicon glyphicon-globe" aria-hidden="true"></span> Explore</a></li>
      </ul>

      <ul class="nav navbar-nav">
        <li><%=link_to "Start a Project", new_project_path  %></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <ul class="nav navbar-nav">
          <% if logged_in? %>
            <li class="avatar" ><%= "Hello, #{current_user.name}!" %></li>
            <li><%= link_to image_tag("default_user.png", size: 28), user_path(current_user.id), class: "avatar", id: "Account" %></li>
            <li><%= link_to "Sign Out", logout_path, method: :delete %></li>
          <% else %>
            <li><%= link_to "Login", login_path %></li>
            <li><%= link_to "Sign Up", new_user_path %></li>
          <% end %>
        </ul>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
